# Copy to vault.yml and encrypt with: ansible-vault encrypt vault.yml
# vault.yml is gitignored — never commit the real file.
vault_k3s_token: "REPLACE-WITH-openssl-rand-hex-32"
vault_tailscale_authkey: "tskey-auth-REPLACE-WITH-TAILSCALE-KEY"
